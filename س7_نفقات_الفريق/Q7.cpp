/*
 * =============================================================
 *  اسم الطالب : عمار الحميري
 *  رقم السؤال : 7
 *  وصف البرنامج: يستخدم مصفوفة ثنائية الأبعاد لتمثيل نفقات فريق
 *               الصفوف = أنواع النفقات (إيجار، معدات، سفر)
 *               الأعمدة = الأشهر (12 شهر)
 *               يحسب مجموع النفقات لكل نوع والمجموع الكلي
 * =============================================================
 */

#include <iostream>
using namespace std;

int main() {

    cout << "===========================================" << endl;
    cout << "   برنامج إدارة نفقات فريق رياضي" << endl;
    cout << "===========================================" << endl;

    const int EXPENSE_TYPES = 3; // أنواع النفقات
    const int MONTHS        = 12; // عدد الأشهر

    double expenses[EXPENSE_TYPES][MONTHS];

    string expenseNames[3] = {"الإيجار  ", "المعدات  ", "السفر    "};
    string monthNames[12]  = {"يناير", "فبراير", "مارس", "أبريل",
                               "مايو", "يونيو", "يوليو", "أغسطس",
                               "سبتمبر", "أكتوبر", "نوفمبر", "ديسمبر"};

    // ------- الخطوة 1: إدخال النفقات -------
    cout << "\nأدخل قيمة النفقات لكل شهر:\n" << endl;
    for (int e = 0; e < EXPENSE_TYPES; e++) {
        cout << "--- نوع النفقة: " << expenseNames[e] << " ---" << endl;
        for (int m = 0; m < MONTHS; m++) {
            cout << "  " << monthNames[m] << ": ";
            cin >> expenses[e][m];
        }
        cout << endl;
    }

    // ------- الخطوة 2: حساب وعرض النتائج -------
    cout << "\n===========================================" << endl;
    cout << "            تقرير النفقات السنوية" << endl;
    cout << "===========================================" << endl;

    double grandTotal = 0;

    for (int e = 0; e < EXPENSE_TYPES; e++) {
        double rowSum = 0;
        cout << "\n[ " << expenseNames[e] << " ]" << endl;

        for (int m = 0; m < MONTHS; m++) {
            cout << "  " << monthNames[m] << ": " << expenses[e][m] << " ريال" << endl;
            rowSum += expenses[e][m];
        }

        cout << "  ----------------------------------------" << endl;
        cout << "  مجموع " << expenseNames[e] << ": " << rowSum << " ريال" << endl;
        grandTotal += rowSum;
    }

    cout << "\n===========================================" << endl;
    cout << "  💰 المجموع الكلي لجميع النفقات: "
         << grandTotal << " ريال" << endl;
    cout << "===========================================" << endl;

    return 0;
}
