/*
 * =============================================================
 *  اسم الطالب : عمار الحميري
 *  رقم السؤال : 1
 *  وصف البرنامج: يطلب من المستخدم إدخال أرقام في مصفوفة،
 *               ثم يحسب عدد مرات تكرار كل رقم ويعرض النتائج
 * =============================================================
 */

#include <iostream>
using namespace std;

int main() {

    // ------- الخطوة 1: تحديد حجم المصفوفة -------
    int n;
    cout << "======================================" << endl;
    cout << "   برنامج حساب تكرار الأرقام في المصفوفة" << endl;
    cout << "======================================" << endl;
    cout << "أدخل عدد العناصر: ";
    cin >> n;

    // ------- الخطوة 2: إدخال عناصر المصفوفة -------
    int arr[100];
    cout << "\nأدخل " << n << " رقم:" << endl;
    for (int i = 0; i < n; i++) {
        cout << "  العنصر [" << i + 1 << "]: ";
        cin >> arr[i];
    }

    // ------- الخطوة 3: عرض المصفوفة المدخلة -------
    cout << "\n--- المصفوفة المدخلة ---" << endl;
    cout << "[ ";
    for (int i = 0; i < n; i++) {
        cout << arr[i];
        if (i < n - 1) cout << ", ";
    }
    cout << " ]" << endl;

    // ------- الخطوة 4: حساب عدد مرات تكرار كل رقم -------
    // نستخدم مصفوفة منطقية لتتبع العناصر التي تم حسابها
    bool alreadyCounted[100] = {false};

    cout << "\n--- نتائج التكرار ---" << endl;
    cout << "الرقم\t|\tعدد مرات التكرار" << endl;
    cout << "------\t|\t----------------" << endl;

    for (int i = 0; i < n; i++) {
        // إذا لم يتم حساب هذا العنصر من قبل
        if (!alreadyCounted[i]) {
            int count = 1; // يبدأ العد من 1 لأن العنصر نفسه يُحسب

            // البحث عن نفس الرقم في بقية المصفوفة
            for (int j = i + 1; j < n; j++) {
                if (arr[i] == arr[j]) {
                    count++;
                    alreadyCounted[j] = true; // تحديد أنه تم حسابه
                }
            }

            alreadyCounted[i] = true;
            cout << "  " << arr[i] << "\t|\t    " << count << endl;
        }
    }

    cout << "\n======================================" << endl;
    return 0;
}
