/*
 * =============================================================
 *  اسم الطالب : عمار الحميري
 *  رقم السؤال : 2
 *  وصف البرنامج: يجد جميع الأرقام الفردية من مصفوفة
 *               ويضيفها إلى مصفوفة جديدة ثم يعرضها
 * =============================================================
 */

#include <iostream>
using namespace std;

int main() {

    // ------- إدخال المصفوفة الأصلية -------
    int n;
    cout << "======================================" << endl;
    cout << "   برنامج استخراج الأرقام الفردية" << endl;
    cout << "======================================" << endl;
    cout << "أدخل عدد العناصر: ";
    cin >> n;

    int arr[100];
    cout << "\nأدخل " << n << " رقم:" << endl;
    for (int i = 0; i < n; i++) {
        cout << "  العنصر [" << i + 1 << "]: ";
        cin >> arr[i];
    }

    // ------- عرض المصفوفة الأصلية -------
    cout << "\n--- المصفوفة الأصلية ---" << endl;
    cout << "[ ";
    for (int i = 0; i < n; i++) {
        cout << arr[i];
        if (i < n - 1) cout << ", ";
    }
    cout << " ]" << endl;

    // ------- إيجاد الأرقام الفردية ونسخها لمصفوفة جديدة -------
    int oddArr[100]; // مصفوفة الأرقام الفردية
    int oddCount = 0; // عداد الأرقام الفردية

    for (int i = 0; i < n; i++) {
        // الرقم فردي إذا كان باقي قسمته على 2 يساوي 1
        if (arr[i] % 2 != 0) {
            oddArr[oddCount] = arr[i];
            oddCount++;
        }
    }

    // ------- عرض الأرقام الفردية -------
    cout << "\n--- الأرقام الفردية ---" << endl;
    if (oddCount == 0) {
        cout << "لا توجد أرقام فردية في المصفوفة." << endl;
    } else {
        cout << "عدد الأرقام الفردية: " << oddCount << endl;
        cout << "[ ";
        for (int i = 0; i < oddCount; i++) {
            cout << oddArr[i];
            if (i < oddCount - 1) cout << ", ";
        }
        cout << " ]" << endl;
    }

    cout << "\n======================================" << endl;
    return 0;
}
