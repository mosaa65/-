/*
 * =============================================================
 *  اسم الطالب : عمار الحميري
 *  رقم السؤال : 6
 *  وصف البرنامج: يستخدم مصفوفة ثنائية الأبعاد لتخزين درجات الحرارة
 *               لأيام الأسبوع خلال الشهر (4 أسابيع × 7 أيام)
 *               ثم يحسب ويعرض متوسط درجة الحرارة لكل أسبوع
 * =============================================================
 */

#include <iostream>
using namespace std;

int main() {

    cout << "========================================" << endl;
    cout << "   برنامج متوسط درجات الحرارة الأسبوعية" << endl;
    cout << "========================================" << endl;

    const int WEEKS = 4; // 4 أسابيع في الشهر
    const int DAYS  = 7; // 7 أيام في الأسبوع

    double temp[WEEKS][DAYS]; // مصفوفة درجات الحرارة

    string dayNames[7] = {"الأحد", "الاثنين", "الثلاثاء",
                           "الأربعاء", "الخميس", "الجمعة", "السبت"};

    // ------- الخطوة 1: إدخال درجات الحرارة -------
    cout << "\nأدخل درجات الحرارة لكل يوم في كل أسبوع:\n" << endl;
    for (int w = 0; w < WEEKS; w++) {
        cout << "--- الأسبوع " << w + 1 << " ---" << endl;
        for (int d = 0; d < DAYS; d++) {
            cout << "  " << dayNames[d] << ": ";
            cin >> temp[w][d];
        }
        cout << endl;
    }

    // ------- الخطوة 2: عرض جدول درجات الحرارة -------
    cout << "--- جدول درجات الحرارة ---" << endl;
    cout << "\t\t";
    for (int d = 0; d < DAYS; d++) {
        cout << dayNames[d] << "\t";
    }
    cout << endl;

    for (int w = 0; w < WEEKS; w++) {
        cout << "الأسبوع " << w + 1 << "\t";
        for (int d = 0; d < DAYS; d++) {
            cout << temp[w][d] << "\t\t";
        }
        cout << endl;
    }

    // ------- الخطوة 3: حساب وعرض المتوسط لكل أسبوع -------
    cout << "\n--- متوسط درجات الحرارة الأسبوعية ---" << endl;

    for (int w = 0; w < WEEKS; w++) {
        double sum = 0;
        for (int d = 0; d < DAYS; d++) {
            sum += temp[w][d]; // جمع كل أيام الأسبوع
        }
        double average = sum / DAYS;
        cout << "  الأسبوع " << w + 1 << ": "
             << average << " درجة مئوية" << endl;
    }

    cout << "\n========================================" << endl;
    return 0;
}
