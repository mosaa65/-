/*
 * =============================================================
 *  اسم الطالب : عمار الحميري
 *  رقم السؤال : 4
 *  وصف البرنامج: يتحقق إذا كانت جميع عناصر المصفوفة
 *               ثنائية الأبعاد (3×3) فريدة أم مكررة
 * =============================================================
 */

#include <iostream>
using namespace std;

int main() {

    cout << "======================================" << endl;
    cout << "   برنامج التحقق من فرادة عناصر المصفوفة 3×3" << endl;
    cout << "======================================" << endl;

    const int ROWS = 3;
    const int COLS = 3;
    int matrix[ROWS][COLS];

    // ------- الخطوة 1: إدخال المصفوفة ثنائية الأبعاد -------
    cout << "\nأدخل عناصر المصفوفة (3 صفوف × 3 أعمدة):\n" << endl;
    for (int i = 0; i < ROWS; i++) {
        for (int j = 0; j < COLS; j++) {
            cout << "  matrix[" << i << "][" << j << "]: ";
            cin >> matrix[i][j];
        }
        cout << endl;
    }

    // ------- الخطوة 2: عرض المصفوفة -------
    cout << "--- المصفوفة المدخلة ---" << endl;
    for (int i = 0; i < ROWS; i++) {
        cout << "  [ ";
        for (int j = 0; j < COLS; j++) {
            cout << matrix[i][j];
            if (j < COLS - 1) cout << ", ";
        }
        cout << " ]" << endl;
    }

    // ------- الخطوة 3: التحقق من فرادة العناصر -------
    // نحول المصفوفة لمصفوفة أحادية الأبعاد لتسهيل المقارنة
    int flat[ROWS * COLS]; // 9 عناصر
    int k = 0;
    for (int i = 0; i < ROWS; i++) {
        for (int j = 0; j < COLS; j++) {
            flat[k++] = matrix[i][j];
        }
    }

    bool isUnique = true;
    // مقارنة كل عنصر مع بقية العناصر
    for (int i = 0; i < ROWS * COLS; i++) {
        for (int j = i + 1; j < ROWS * COLS; j++) {
            if (flat[i] == flat[j]) {
                isUnique = false;
                cout << "\n⚠️  التكرار: الرقم " << flat[i]
                     << " مكرر في المصفوفة!" << endl;
            }
        }
    }

    // ------- الخطوة 4: عرض النتيجة النهائية -------
    cout << "\n--- النتيجة ---" << endl;
    if (isUnique) {
        cout << "✅ جميع العناصر فريدة - لا يوجد تكرار!" << endl;
    } else {
        cout << "❌ العناصر ليست فريدة - يوجد تكرار!" << endl;
    }

    cout << "\n======================================" << endl;
    return 0;
}
